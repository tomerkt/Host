import 'package:flutter/material.dart';

class HostOffersList extends StatelessWidget {
  const HostOffersList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('host offers'),
      ),
    );
  }
}
